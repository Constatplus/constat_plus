enum MissionType {
  entry,
  exit,
  beforeWorks,
  afterWorks,
  provisionalAcceptance,
  finalAcceptance,
}

enum PropertyType {
  apartment,
  house,
  studio,
  commercial,
  office,
  garage,
  building,
  other,
}

enum MissionStatus {
  draft,
  inProgress,
  completed,
  archived,
}

enum ObservationSeverity {
  info,
  minor,
  medium,
  major,
}